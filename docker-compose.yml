services:
  backend:
    build:
      context: ./backend
      target: runtime
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
      target: runtime
    ports:
      - "4200:80"
    depends_on:
      - backend
